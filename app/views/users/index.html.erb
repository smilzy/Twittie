<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<% # Można zostawić samo <ul> <%= render @users % > </ul> i Rails to ogarnie %>

<ul class="users">
  <% @users.each do |the_user| %>
    <%= render 'users/user', user: the_user %>
  <% end %>
</ul>

<%= will_paginate %>