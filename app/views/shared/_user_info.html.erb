<%= link_to image_tag(current_user.avatar, size: 100), current_user if current_user.avatar? %>
<%= link_to gravatar_for(current_user, size: 50), current_user unless current_user.avatar? %>

<h1><%= current_user.name %></h1>
<span><%= link_to "view my profile", current_user %></span>
<span><%= pluralize(current_user.microposts.count, "post") %></span>